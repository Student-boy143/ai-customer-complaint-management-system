# Application
APP_NAME=AI Complaint Management System
APP_VERSION=0.1.0
DEBUG=true

# Server
HOST=0.0.0.0
PORT=8000

# CORS — comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Database (configure in a future milestone)
DATABASE_URL=mysql+pymysql://user:password@localhost:3306/complaints_db

# Groq API (configure in a future milestone)
GROQ_API_KEY=
